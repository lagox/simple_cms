<%= link_to("<< Back to list", {:action => 'list', :page_id => @page.id}, :class => "back-list") %>

<div class="section delete">
  <h2>Section Subject</h2>
  <%= form_for(:section, :url => {:action => 'destroy', :id => @section.id, :page_id => @page.id}) do |f| -%>
  <p>Are you sure want to permanently delete this subject?</p>
  <p class="reference-name"><%= @section.name %></p>
  <div class="form-buttons">
      <%= submit_tag("Delete Subject") %>
  </div>
  <% end %>
</div>